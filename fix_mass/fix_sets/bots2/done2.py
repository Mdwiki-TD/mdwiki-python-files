"""

from fix_mass.fix_sets.bots2.done2 import find_done_study #find_done_study(title)

"""
from newapi.ncc_page import CatDepth

already_done22 = CatDepth("Category:Sort studies fixed", sitecode="www", family="nccommons", depth=0)


def find_done_study(title):
    if title in already_done22:
        return True
    return False
